Created by Tyler D'Alessandro

Time complexity O(n)

To use:
1) Enter desired array size and values in the main function.
2) Enter desired sum to be searched for.

Once the function is run, it will return the indices of the two values
that add up to the desired sum. If no valid values are found, it
will return -1.
